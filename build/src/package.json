{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "npm i && cp -r ../dist/* . && node build-win && sh src/remove.sh"
  },
  "author": "",
  "devDependencies": {
    "electron-builder": "^22.4.1",
    "electron": "^8.1.1"
  }
}
